# Debugging CFP Beam Calculation – 2026-05-30

## Summary
Today was spent tracking down why the CFP (Common Focal Point) source beam calculation in the Roll QGIS plugin was producing incorrect results. The process was slow and frustrating due to legacy error handling, misleading diagnostic output, and uncertainty about which code path was actually being executed.

## Key Events
- Initial focus was on missing or misleading error messages during CFP analysis.
- Confirmed that the correct function (`compute_monochromatic_weighted_beam_xy_grid`) was being called, after removing a leftover test exception.
- Updated error handling in the worker thread wrappers to show real exceptions and tracebacks, removing outdated Numba-related messages.
- Encountered NumbaPerformanceWarnings, but these were not the root cause of the beam issues.
- Realized that the real problem was not in error handling, but in the calculation logic or input data.
- Next step is to inspect and compare the actual input arrays to the beam calculation to diagnose why the output is still wrong.

## Lessons Learned
- Legacy error messages and test code can obscure the real source of problems.
- Always confirm which function is actually being called before debugging logic.
- Good diagnostics and clear error reporting are essential for debugging complex numerical code.

## Next Steps
- Add diagnostics to inspect input arrays and intermediate values in the beam calculation.
- Compare weighted and unweighted beam outputs for the same input.
- Review input data for possible issues.

---

*Session note generated by GitHub Copilot on request, following the expected markdown format for handoff and review.*
