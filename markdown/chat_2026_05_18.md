# Session Handoff - 2026-05-18

## Status

The recent geometry stabilization slice is landed and should be treated as maintenance-complete for refactoring purposes.

- Template-roll offsets are now restricted to `rollingGrid`.
- `fixedGrid`, `circle`, `spiral`, and `well` seeds are invariant under template roll.
- Well-borehole receiver geometry generation is fixed and covered by regression tests.
- Geometry dispatch now mirrors binning dispatch: `geomTemplate4()` is the stable/default path and `geomTemplate5()` is re-enabled behind `getActiveAppSettings().useExperimental`.
- The unsafe relation gap splitting idea was removed after it proved risky on sparse real numbering.

## Test Coverage Added

Direct `geomTemplate5()` equivalence coverage now includes:

- rolled-grid geometry
- invariant well receivers
- invariant circle/spiral receivers
- receiver border filtering
- coincident receiver cells across blocks

## Roadmap Update

`Refactoring_roadmap.txt` was updated on 2026-05-18 to reflect the current state.

## Recommended Next Step

Do not reopen a broad `roll_survey.py` refactor next unless the goal is algorithm comparison or promoting experimental geometry/binning paths after more evidence.

The best next refactoring slice is to extract the Layout-tab 2D-to-3D bridge from `roll_main_window_create_layout_tab.py` into a narrow bridge module while keeping `layout_3D.py` rendering-focused and preserving the `Layout3DWidget.updateFromSurvey()` contract.
